# @crypto/cipher-contract

Types and stable API surface for encryption/decryption logic used across apps.

This package only exports TypeScript types and minimal helpers; implementation lives in `packages/cipher-core`.

Usage:

```ts
import {
  CipherContract,
  CipherResult,
  BaseInput,
} from "@crypto/cipher-contract";
```
