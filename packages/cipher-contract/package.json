{
  "name": "@crypto/cipher-contract",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests configured'"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
