# Next Update - v1.2.x

## Mobile

- Add fractional number-system conversion support in Base Converter (for example `10.625`).
- Include full step-by-step fractional conversion for each target base.
- Add precision controls (max fractional digits and rounding mode).
- Add copy/share actions for conversion output blocks.

## Process

- Continue appending patch notes under `packages/patch-notes/versions/` per version.
- Keep web/mobile parity items tracked separately from mobile-only runtime work.
